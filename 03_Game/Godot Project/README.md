# Godot Project

Godot project root workspace.

Use this folder for project files, project settings, addons, export presets, build configuration, and the runtime root once the Godot project is initialized.
